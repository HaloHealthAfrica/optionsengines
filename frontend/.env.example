# Frontend Environment Variables

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8080
# For production, set this to your Fly.io backend URL:
# NEXT_PUBLIC_API_URL=https://optionsengines.fly.dev

# JWT Secret (for frontend auth token validation)
JWT_SECRET=your-secret-key-min-32-chars-change-in-production

# Demo credentials (fallback if backend auth fails)
DEMO_EMAIL=demo@optionagents.ai
DEMO_PASSWORD=demo
